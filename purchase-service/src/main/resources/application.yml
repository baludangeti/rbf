server:
  port: 9109

spring:
  application:
    name: purchase-service
  datasource:
    url: jdbc:h2:file:./data/purchase-db;AUTO_SERVER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

services:
  inventory-service:
    base-url: http://localhost:9106
